# pb

Swipeable photo stack built with Vite, React, Chakra UI 2, Motion, and Font Awesome.

```sh
npm install
npm run optimize-images
npm run dev
```

```sh
npx playwright install chromium
npm run test:e2e
```
